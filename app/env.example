# Variáveis de ambiente da aplicação
# Renomeie este arquivo para ".env" em ambiente de desenvolvimento
# Em produção, use secrets/variáveis do ambiente (não commitar .env)

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Exemplo de DATABASE_URL (montada automaticamente pelo app quando as variáveis acima existem)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app


